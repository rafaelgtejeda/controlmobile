
abstract class OrdemServicoScreenContract {
  void onOrdemServicoSuccess(dynamic resposta);
  void onOrdemServicoError(String errorTxt);
}

class OrdemServicoScreenPresenter {
  // OrdemServicoScreenContract _view;
  // OrdemServicoService api = new OrdemServicoService();
  // OrdemServicoScreenPresenter(this._view);

  getListaOrdemServicoGrid() {
  //   api.oSProximosChamadosLista().then((resposta) {
  //     _view.onOrdemServicoSuccess(resposta);
  //   }).catchError((Object error) => _view.onOrdemServicoError(error.toString()));
  
 }
}